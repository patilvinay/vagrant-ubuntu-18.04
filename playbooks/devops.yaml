- hosts: localhost
  connection: local
  vars:
    the_user: "{{ ansible_user_id }}"
  become: true
  become_method: sudo
  roles:
    - geerlingguy.pip
    - geerlingguy.docker
    - geerlingguy.kubectl
    #- ansible-role-kubectl
 


    